Planner runs the monitor. The running monitor receives the image data from a ROS node. 
It interprets parts of the world related to the input parameter. It knows what to look for. 
If any change happens it notifies the planner. 